import { Component } from '@angular/core';

@Component({
  selector: 'app-update-iamges-of-destination',
  standalone: true,
  imports: [],
  templateUrl: './update-iamges-of-destination.component.html',
  styleUrl: './update-iamges-of-destination.component.css'
})
export class UpdateIamgesOfDestinationComponent {
  
  // skills: new FormArray([]),

  // addskill() {
  //   this.skills.push(
  //     new FormControl('', [
  //       Validators.required,
  //       Validators.pattern('^[a-zA-Z0-9\\s]{3,50}$'),
  //       Validators.minLength(3),
  //       Validators.maxLength(50),
  //     ])
  //   );
  // }
  // removeskill(i: number) {
  //   this.skills.removeAt(i);
  // }
}
